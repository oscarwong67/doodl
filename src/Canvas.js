//this will be the new canvas component to replace the current one
//it should keep as much functionality as possible in that it should:
//allow the user to change colours and undo
//it should support live-streaming of drawings w/ minimal bandwidth hit - ToDo: figure out how that'll work
//it should be easily cleared
import React, { Component } from 'react';

class Canvas extends Component {

}

export default Canvas;